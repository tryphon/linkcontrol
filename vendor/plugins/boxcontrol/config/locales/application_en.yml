en:
  activerecord:
    attributes:
      network:
        method: Configuration
        static_address: IP address
        static_netmask: Network mask
        static_gateway: Gateway
        static_dns1: DNS server
      link_stream:
        target_host: Target host
        target_port: Target port
        http_port: Local HTTP port
        udp_port: Local UDP port
    models:
      link_stream: Link configuration
      network: Network configuration
      save_point: Save configuration
  networks:
    method:
      static: Manual configuration
      dhcp: Automatic configuration (via DHCP)
    show:
      static_properties: Manual parameters
    edit:
      title: Edit the network configuration
      submit: Edit
      cancel: back
  link_streams:
    target_properties: Stream target
    local_ports_properties: Local ports
    edit:
      title: Edit the link configuration
      submit: Edit
      cancel: back
  save_points:
    new:
      submit: Save
  layouts:
    application:
      dashboard: Dashboard
      help: Help
      information: 
        title: Information
        text: | 
          For more information about <a href="http://www.tryphon.eu/fr/produits/linkbox">LinkBox</a>, please visit our website: <a href="http://www.tryphon.eu/fr">tryphon.eu</a>
  or: or
  actions:
    edit: Edit
