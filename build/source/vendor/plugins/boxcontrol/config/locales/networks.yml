en:
  activerecord:
    attributes:
      network:
        method: Configuration
        static_address: IP address
        static_netmask: Network mask
        static_gateway: Gateway
        static_dns1: DNS server
        address: Current IP address
        id: Identifiant
        mac_address: Mac Address
    errors:
      models:
        network:
          not_a_valid_ip_address: "is not a valid IP address"
          can_be_the_static_address: "can't the box IP address itself"
          attributes:
            static_netmask:
              not_a_valid_netmask: "is not a valid network mask"
            static_gateway:
              not_in_local_network: "is not in the local network"
    models:
      network: Network configuration
  networks:
    undefined_address: undefined
    method:
      static: Manual configuration
      dhcp: Automatic configuration (via DHCP)
    name:
      eth0: Primary interface
      eth1: Secondary interface
      wlan0: Wifi interface
      vlan100: VLAN 100 interface
    show:
      static_properties: Manual parameters
      physical_properties: Physical parameters
    edit:
      title: Edit the network configuration
      submit: Edit
      cancel: back
    index:
      title: Network configuration

fr:
  activerecord:
    attributes:
      network:
        method: Configuration
        static_address: Adresse IP
        static_netmask: Masque réseau
        static_gateway: Passerelle
        static_dns1: Serveur de nom
        address: Adresse IP actuelle
        id: Identifiant
        mac_address: Adresse MAC
    errors:
      models:
        network:
          not_a_valid_ip_address: "n'est pas une adresse IP valide"
          can_be_the_static_address: "ne peut être l'adresse IP de la box"
          attributes:
            static_netmask:
              not_a_valid_netmask: "n'est pas un masque réseau valide"
            static_gateway:
              not_in_local_network: "n'est pas dans le réseau local"
    models:
      network: Configuration réseau    
  networks:
    undefined_address: "non définie"
    method:
      static: Configuration manuelle
      dhcp: Configuration automatique (via DHCP)
    name:
      eth0: Interface principale
      eth1: Interface auxiliaire
      wlan0: Interface sans-fil (Wifi)
      vlan100: Interface avec le VLAN 100
    show:
      static_properties: Paramètres manuels
      physical_properties: Paramètres physiques
    edit:
      title: Modifier la Configuration réseau
      submit: Modifier
      cancel: revenir
    index:
      title: Configuration réseau
